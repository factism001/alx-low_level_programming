#include "main.h"
/**
  * _strlen - returns the length of a string.
  * @s: input string.
  * Return: lenght of string
  */
int _strlen(char *s)
{
	int lenght;
	int *s;

		lenght = _strlen(*s);

		return (lenght);
}
