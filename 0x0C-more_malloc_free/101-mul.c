#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
  * main - program that multiplies two numbers
  * Return: Always 0
  */
int main(void)
{
	int i, j;

	int i = num1;
	int j = num2;


	if (!isdigit(i || j))
	{
		printf("Error\n");
		exit(98);
	}

	if (i < 0 || j < 0)

	{
		printf("Error\n");
		exit(98);
	}

	#define mul(i, j) ((i) * (j))

	printf("%d\n", mul(i, j);

	return (0);
}
