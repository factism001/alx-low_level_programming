#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
  * main - program that multiplies two numbers
  * Return: Always 0
  */
int main(void)
{
	int num1;
	int num2;


	if (!isdigit(num1 || num2))
	{
		printf("Error\n");
		exit(98);
	}

	if (num1 < 0 || num2 < 0)

	{
		printf("Error\n");
		exit(98);
	}

	#define mul(num1, num2) ((num1) * (num2))

	printf("%d\n", mul);

	return (0);
}
