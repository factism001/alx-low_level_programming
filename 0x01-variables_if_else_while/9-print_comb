#include <stdio.h>
/**
  * main - main entry
  * description: print all the possible combination of single digit number
  * Return: Always 0
  */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		putchar(num + '0');
		putchar(',');
		putchar(' ');
	}

	putchar('\n');

	return (0);
}
