#include "main.h"
/**
  * print_numbers - prints number from zero to nine with new line
  * Return: 0
  */
void print_numbers(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		_putchar(num);
	}
	_putchar('\n');

	return (0);
}
